# Core Telegram Bot
python-telegram-bot==20.7

# HTTP requests (used for OpenRouter API calls)
requests==2.31.0

# Optional: Image processing (bot works without it, but include for full features)
Pillow==10.1.0

# HTTP client (used by python-telegram-bot internally)
httpx==0.25.2

